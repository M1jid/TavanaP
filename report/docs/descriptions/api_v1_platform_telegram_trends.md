Returns the top Telegram posts that contain a given **keyword**,  
ranked by the highest number of **views** and **forwards**,  
within the specified date range.

Required fields in the payload: `title`, `start_date`, and `end_date`.

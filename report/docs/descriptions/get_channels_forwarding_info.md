Returns a list of channels that have forwarded messages from the specified main channel ID.

The request payload requires `CHANNEL_ID`.

This endpoint helps track which channels have forwarded messages from a given Telegram channel.

Accepts a message keyword, date range, and a limit (`Range`).  
Returns a list of channels sorted by the earliest to latest that posted the message,  
helping identify the original source channel and subsequent forwarding channels.

Accepts a keyword and a time range, searches posts containing the keyword within the specified dates,  
tokenizes the messages, removes stop words, and returns data for generating a word cloud.  

The response includes the count of messages containing the keyword in the given time frame, useful for visualization.

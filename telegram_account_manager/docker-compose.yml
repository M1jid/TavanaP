services:

  telegram_account_manager:
    build: .
    image: telegram_account_manager
    volumes:
      - ../.env:/code/.env
    restart: unless-stopped
    ports:
      - "9000:9000"

networks:
  app-network:
    driver: bridge
